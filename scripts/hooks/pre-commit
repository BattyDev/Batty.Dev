#!/bin/sh
# Auto-regenerate index.html on every commit so the landing page never drifts
# from meta/projects.json. Idempotent — produces no diff if nothing changed.
node scripts/build-index.js || exit 1
git add index.html
